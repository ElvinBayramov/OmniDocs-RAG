chromadb>=0.6.0
sentence-transformers>=3.0.0
fastmcp>=2.0.0
rank-bm25>=0.2.2

# Web Dashboard
fastapi>=0.115
uvicorn>=0.30

# Web crawling & HTML parsing (Phase 4)
httpx>=0.27
beautifulsoup4>=4.12
html2text>=2024.2
lxml>=5.0

# Optional: GPU acceleration (pip install torch)
# Set RAG_DEVICE=cuda to enable

# Optional: JS-rendered sites (pip install playwright && playwright install chromium)

# Optional: Binary document formats
# pip install pypdf python-docx openpyxl python-pptx
# Uncomment below to install automatically:
# pypdf>=4.0
# python-docx>=1.1
# openpyxl>=3.1
# python-pptx>=0.6.23

# Optional: File watcher (auto-reindex on file changes)
# pip install watchdog
# Set RAG_WATCH_PATH and RAG_WATCH_COLLECTION env vars to activate
# watchdog>=4.0
